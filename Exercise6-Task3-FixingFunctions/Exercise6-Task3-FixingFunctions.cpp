Please write your solution here.
For your convenience we have added a template for your answers below.

# Function  `c++|is_even`:

The problem with this function is ...

```c++
// PRE: <i > 0 (unsigned int)>
// POST: <Gives bool true only if its even>
bool is_even(unsigned int i) {
  // TODO: Fix code below.
  //if (i % 2 == 0) return true;
  return i % 2 == 0;
}
```

# Function `c++|invert`:

The problem with this function is ...

```c++
// PRE: <n/a>
// POST: <Gives the inverse of x, or zero if input is zero>
double invert(int x) {
  // TODO: Fix code below.
  double result = 0;
  if (x != 0) {
      result = 1.0 / x;
  }
  return result;
}
```